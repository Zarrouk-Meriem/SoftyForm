import { BaseDownloader } from './base.js';
/**
 * Custom downloader
 *
 * Class extending this downloader must implement:
 * - constructor()
 * - _init()
 * - _checkForUpdate()
 * - _loadData()
 */
export declare class CustomDownloader<DataType> extends BaseDownloader<DataType> {
    /**
     * Load content
     */
    _loadContent(): Promise<void>;
}
