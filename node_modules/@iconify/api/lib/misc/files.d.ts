import type { FileEntry } from '../types/files.js';
/**
 * List all files in directory
 */
export declare function listFilesInDirectory(path: string): Promise<FileEntry[]>;
/**
 * Hash files to quickly check if files were changed
 *
 * Does not check file contents, checking last modification time should be enough
 */
export declare function hashFiles(files: FileEntry[]): string;
/**
 * Check if directory exists
 */
export declare function directoryExists(dir: string): Promise<boolean>;
/**
 * Add '/' to start of filename
 */
export declare function prependSlash(filename: string): string;
