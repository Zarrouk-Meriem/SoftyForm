import type { IconifyJSON, IconifyAliases } from '@iconify/types';
import type { StoredIconSet } from '../../../types/icon-set/storage.js';
/**
 * Get list of icons that must be retrieved
 */
export declare function getIconsToRetrieve(iconSet: StoredIconSet, names: string[], copyTo?: IconifyAliases): Set<string>;
/**
 * Get icons from stored icon set
 */
export declare function getStoredIconsData(iconSet: StoredIconSet, names: string[], callback: (data: IconifyJSON) => void): void;
