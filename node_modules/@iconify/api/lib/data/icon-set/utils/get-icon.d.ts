import type { ExtendedIconifyIcon, IconifyIcons } from '@iconify/types';
import type { SplitIconifyJSONMainData } from '../../../types/icon-set/split.js';
import type { StoredIconSet } from '../../../types/icon-set/storage.js';
/**
 * Get icon data
 *
 * Assumes that icon exists and valid. Should validate icon set and load data before running this function
 */
export declare function getIconData(data: SplitIconifyJSONMainData, name: string, icons: IconifyIcons): ExtendedIconifyIcon;
/**
 * Get icon data from stored icon set
 */
export declare function getStoredIconData(iconSet: StoredIconSet, name: string, callback: (data: ExtendedIconifyIcon | null) => void): void;
