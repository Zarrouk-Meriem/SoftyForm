import type { MemoryStorage, MemoryStorageConfig, MemoryStorageItem } from '../../types/storage.js';
/**
 * Create storage
 */
export declare function createStorage<T>(config: MemoryStorageConfig): MemoryStorage<T>;
/**
 * Create item to store
 */
export declare function createStoredItem<T>(storage: MemoryStorage<T>, data: T, cacheFile: string, autoCleanup?: boolean, done?: (storedItem: MemoryStorageItem<T>, err?: NodeJS.ErrnoException) => void): MemoryStorageItem<T>;
