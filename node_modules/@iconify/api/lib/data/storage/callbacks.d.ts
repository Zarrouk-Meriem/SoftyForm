import type { MemoryStorageItem } from '../../types/storage.js';
/**
 * Run all callbacks from storage
 */
export declare function runStorageCallbacks<T>(storedItem: MemoryStorageItem<T>, force?: boolean): void;
