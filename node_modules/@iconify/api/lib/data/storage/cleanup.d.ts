import type { MemoryStorage, MemoryStorageItem } from '../../types/storage.js';
/**
 * Clean up stored item
 */
export declare function cleanupStoredItem<T>(storage: MemoryStorage<T>, storedItem: MemoryStorageItem<T>): boolean;
/**
 * Clean up stored items
 */
export declare function cleanupStorage<T>(storage: MemoryStorage<T>): void;
/**
 * Add storage to cleanup queue
 *
 * Should be called after writeStoredItem() or loadStoredItem()
 */
export declare function addStorageToCleanup<T>(storage: MemoryStorage<T>, storedItem: MemoryStorageItem<T>): void;
